document.addEventListener('WebComponentsReady', function () {

  describe('<seed-component>', function () {

    it('The Structure is correct', function () {
      assert.equal(true, true);
    });
  });
});
